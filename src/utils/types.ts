export type ErrorForm = {
    name: string,
    phone: string,
    email: string,
    message: string,
    genericError: string
}

export type TouchedForm = {
    name: boolean,
    phone: boolean,
    email: boolean,
    message: boolean
}